name: build-deb-ubuntu-26-04
description: Build the Ubuntu 26.04 DEB package.
runs:
  using: composite
  steps:
    - name: Install build dependencies
      shell: bash
      run: |
        apt-get update
        apt-get install -y \
         bash git curl wget build-essential file pkg-config \
         libssl-dev libwebkit2gtk-4.1-dev libgtk-3-dev \
         libayatana-appindicator3-dev librsvg2-dev libsoup-3.0-dev \
         patchelf python3 ca-certificates \
         libvips-dev libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev
        curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
        apt-get install -y nodejs

    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust
      shell: bash
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain "${RUST_VERSION}"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        . "$HOME/.cargo/env"
        rustc --version
        cargo --version

    - name: Cache Cargo
      uses: actions/cache@v4
      with:
        path: |
         ~/.cargo/bin/
         ~/.cargo/registry/index/
         ~/.cargo/registry/cache/
         ~/.cargo/git/db/
         src-tauri/target/
        key: deb-ubuntu2604-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
        restore-keys: deb-ubuntu2604-cargo-

    - name: Clone WebClients
      shell: bash
      run: |
        GIT_TERMINAL_PROMPT=0 git clone --depth=1 --single-branch --branch "${WEBCLIENTS_REF}" https://github.com/ProtonMail/WebClients.git WebClients

    - name: Apply distro patch
      shell: bash
      run: |
        DISTRO_PATCH="${{ github.event.inputs.distro_patch || 'ubuntu.26.04' }}"
        PATCH_FILE="patches/deb/${DISTRO_PATCH}.patch"
        if [ -f "$PATCH_FILE" ]; then
            git apply "$PATCH_FILE"
            echo "Applied $PATCH_FILE"
        else
            echo "WARNING: $PATCH_FILE not found, building without distro patch"
        fi

    - name: Build DEB
      shell: bash
      run: |
            . "$HOME/.cargo/env"
            rm -rf src-tauri/target/release/bundle/deb
            scripts/build-webclients.sh
            VERSION=$(node -p "require('./package.json').version")
            sed -i "s/\"version\": \"[^\"]*\"/\"version\": \"$VERSION\"/" src-tauri/tauri.conf.json
            sed -i "0,/^version = \"[^\"]*\"/s//version = \"$VERSION\"/" src-tauri/Cargo.toml
            npm install
            npx tauri build --bundles deb --verbose

    - name: Normalize DEB filename
      shell: bash
      run: |
        VERSION=$(node -p "require('./package.json').version")
        cd src-tauri/target/release/bundle/deb
        for file in *.deb; do
          if [ -f "$file" ]; then
            target="proton-drive_${VERSION}_${DISTRO_SUFFIX}_amd64.deb"
            if [ "$file" != "$target" ]; then
              mv "$file" "$target"
            fi
          fi
        done

    - name: Write artifact manifest
      shell: bash
      run: |
        ARTIFACT_MANIFEST_DIR=src-tauri/target/release/bundle/deb \
          bash scripts/ci/write-artifact-manifest.sh \
          deb-package-ubuntu2604 \
          deb \
          ubuntu-26.04 \
          x86_64 \
          "src-tauri/target/release/bundle/deb/proton-drive_*_ubuntu26.04_amd64.deb"

    - name: Upload DEB artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
      with:
        name: deb-package-ubuntu2604
        path: |
          src-tauri/target/release/bundle/deb/proton-drive_*_ubuntu26.04_amd64.deb
          src-tauri/target/release/bundle/deb/deb-package-ubuntu2604.*
        if-no-files-found: error
        retention-days: 30
